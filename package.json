{
  "name": "board-mirror",
  "version": "0.1.0",
  "description": "掲示板をスクレイピングして Cloudflare Workers 上で再表示するサイト",
  "type": "module",
  "private": true,
  "scripts": {
    "scrape": "node --env-file-if-exists=.env scraper/index.mjs",
    "notify": "node --env-file-if-exists=.env scraper/notify.mjs",
    "post": "node --env-file-if-exists=.env poster/index.mjs",
    "batch": "npm run scrape && npm run notify && npm run post",
    "dev": "sh -c 'if [ -f .env ]; then npx wrangler dev --env-file .env --port 9000; else npx wrangler dev --port 9000; fi'",
    "setup": "sh -c 'echo \"$GH_TOKEN\" | gh auth login --with-token && gh auth setup-git && git config user.name \"egoh_1193\" && git config user.email \"egoh_1193@users.noreply.github.com\" && ([ -d node_modules ] || npm install)'",
    "deploy": "npx wrangler deploy",
    "gh-action-test": "npm run batch"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "wrangler": "^4.129.0"
  }
}
