# AGENTS.md

このリポジトリで作業するすべての AI エージェント(Claude Code など)向けの厳守ルール。詳細な開発ガイドは CLAUDE.md を参照。

## ユーザー設定ファイルの取り扱い(厳守)

`.env` / `.dev.vars` / `scraper/config.json` / `.scrape-state.json` / `site/data/` は**ユーザーが実環境の値(トークン・実サイト URL など)を書き込むファイル**。2026-09-05 に、動作検証中に `.env` をテンプレートで上書きし、ユーザーが入力済みのトークンを消失させた実事故が発生している。

1. **これらのファイルを書き込まない**。上書き・削除・テンプレート復元・「戻す」も禁止。読み取りは可(トークン等の値を会話へ出力しない)
2. テスト用の環境変数・設定は **`/tmp` 側に作る**(例: `node --env-file=/tmp/test.env …`)。リポジトリ内の設定ファイルに書かない
3. 「戻す / 初期化 / リストア」はユーザーが明示的に指示したときのみ。実行前に mtime・git status でユーザー編集の痕跡を確認し、疑問があれば実行を中止して確認する
4. ユーザーが編集している可能性のあるファイル(=gitignore されているが実在するもの)を扱うときは、常に「自分が書いていいファイルか」を最初に考える。gitignore されたファイルは大抵ユーザーのローカル状態であり、リポジトリから復元できない

## シェルコマンドの提示(厳守)

ユーザーはコマンドをそのままコピー&ペーストして実行する。

1. 長いコマンドを複数行で示すときは**単純な改行で分断しない**。後半が別コマンドとして実行されて壊れる
2. 改行が必要な場合は行末に継続記号 `\` を付けて複数行にする(例: `git add \
   file1 file2`)
3. 1 行に収まるなら 1 行のまま提示する